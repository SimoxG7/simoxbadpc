#include <stdio.h>

int main(void) {
    char * a = "Simox";
    char c = *a;
    // while (c != '\0') {
    //     printf("%c ", c);
    //     c = *a+1;
    // }
    printf("%c", c);
    return 0;
}